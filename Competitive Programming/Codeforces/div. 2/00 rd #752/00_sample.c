#include <stdio.h>
#include <stdbool.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>
#include <limits.h>

int	main(void)
{
	int	T;
	scanf("%d", &T);

	while (T--)
	{
		long long int	a, b;
		scanf("%lld%lld", &a, &b);

		int	n, k;
		scanf("%d%d", &n, &k);

		char	str[100];
		scanf(" %[^\n]", str);
	}
}
